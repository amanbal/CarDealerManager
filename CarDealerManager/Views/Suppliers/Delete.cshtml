@model CarDealerManager.Models.Supplier

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container">
    <main role="main" class="pb-3">
        <h1>Delete</h1>
        <h3>Are you sure you want to delete this?</h3>
        <div>
            <h4>Supplier</h4>
            <hr />
            @if (@ErrorMessageHandler.ISThereError == "false")
            {
                <dl class="row">
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.SupplierName)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.SupplierName)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Email)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Email)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Phone)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Phone)
                    </dd>
                </dl>
            }
            else
            {
                <h1 style="color:red;">Error Somthing Unexpedited happened</h1>
                <p style="color:red;"><strong>Trouble shooting:</strong>@ErrorMessageHandler.ErrorMessage</p>
                <p style="color:red;"><strong>@ErrorMessageHandler.CustomErrorMessagePart1</strong></p>
                <p style="color:red;"><strong>@ErrorMessageHandler.CustomErrorMessagePart2</strong></p>
            }
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Delete" class="btn btn-danger" /> |
                <a asp-action="Index">Back to List</a>
            </form>
        </div>
    </main>
</div>

